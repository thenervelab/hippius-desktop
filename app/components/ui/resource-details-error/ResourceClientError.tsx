"use client";

import { ResourceDetailsError } from "@/app/components/ui";
import { ResourceDetailsErrorProps } from ".";

export default function ResourceClientError(props: ResourceDetailsErrorProps) {
  return <ResourceDetailsError {...props} />;
}
