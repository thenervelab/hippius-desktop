import { SVGProps, ReactNode } from "react";

export const CheckingIPFS = ({ className }: SVGProps<SVGSVGElement>): ReactNode => (
  <img src="/illustrations/checking-ipfs.svg" alt="" className={className} />
);
export default CheckingIPFS;
