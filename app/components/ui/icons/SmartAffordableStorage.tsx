import { SVGProps, ReactNode } from "react";

export const SmartAffordableStorage = ({ className }: SVGProps<SVGSVGElement>): ReactNode => (
  <img src="/illustrations/smart-affordable-storage.svg" alt="" className={className} />
);
export default SmartAffordableStorage;
