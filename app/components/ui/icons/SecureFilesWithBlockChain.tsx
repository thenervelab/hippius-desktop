import { SVGProps, ReactNode } from "react";

export const SecureFilesWithBlockChain = ({ className }: SVGProps<SVGSVGElement>): ReactNode => (
  <img src="/illustrations/secure-files-with-blockchain.svg" alt="" className={className} />
);
export default SecureFilesWithBlockChain;
