import { SVGProps, ReactNode } from "react";

export const DownloadingIPFS = ({ className }: SVGProps<SVGSVGElement>): ReactNode => (
  <img src="/illustrations/downloading-ipfs.svg" alt="" className={className} />
);
export default DownloadingIPFS;
