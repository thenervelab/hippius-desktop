# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

.next
node_modules
out
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri secrets
src-tauri/.env

# Git worktrees (local development)
.worktrees/

# Local Claude Code state, except the team-shared path-scoped rules that
# extend CLAUDE.md (git cannot re-include a file under an excluded directory,
# so the contents are excluded rather than the directory itself).
.claude/*
!.claude/rules/

tsconfig.tsbuildinfo

# Vitest coverage report (generated by `pnpm test:coverage`)
coverage/

# Git worktrees for parallel branch work (never tracked)
.worktrees/
