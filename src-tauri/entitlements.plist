<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.app-sandbox</key>
    <false/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <!-- Commenting out these entitlements to avoid permission prompts at app startup.
         They will be requested by the file picker dialog when the user actually selects a folder. -->
    <!-- <key>com.apple.security.files.downloads.read-write</key>
    <true/> -->
    <!-- <key>com.apple.security.files.pictures.read-write</key>
    <true/> -->
    <!-- <key>com.apple.security.files.music.read-write</key>
    <true/> -->
    <!-- <key>com.apple.security.files.movies.read-write</key>
    <true/> -->
    <!-- <key>com.apple.security.files.documents.read-write</key>
    <true/> -->
    <!-- <key>com.apple.security.files.desktop.read-write</key>
    <true/> -->
    <key>com.apple.security.files.bookmarks.app-scope</key>
    <true/>
    <key>com.apple.security.files.bookmarks.document-scope</key>
    <true/>
    <!-- Shared App Group container for the Finder Sync extension. The app is
         non-sandboxed and reaches the container by literal path; this
         entitlement is what lets the SANDBOXED extension share it (the IPC
         socket lives there). Team-ID-prefixed => no Apple-portal registration.
         Keep this string identical to macos/FinderSync.entitlements and
         macos/group.env. -->
    <key>com.apple.security.application-groups</key>
    <array>
        <string>V28B5X732P.com.hippius.shared</string>
    </array>
    <!-- allow-jit + allow-unsigned-executable-memory are required by the
         WKWebView / JavaScriptCore JIT under the hardened runtime, so they stay. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- disable-library-validation removed (audit M-11): the app bundles only its
         own signed dylibs, so letting the notarized process load arbitrary
         UNSIGNED libraries was needless attack surface. VERIFY with a signed +
         notarized build that the app still launches before release; if a bundled
         framework needs it, restore this key. -->

    <!-- <key>com.apple.security.files.all</key>
    <dict>
        <key>read</key>
        <true/>
        <key>write</key>
        <true/>
    </dict>
    <key>com.apple.security.temporary-exception.files.absolute-path.read-write</key>
    <array>
        <string>/</string>
    </array> -->
</dict>
</plist>